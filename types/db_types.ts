export type user_edits = {
    username : string,
    password : string,
    old_password : string,
    id : number
};

export type list_edits = {
    visibility_level : number,
    list_name : string,
    id : number
}

